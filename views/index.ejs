<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Homepage</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <h1>Homepage</h1>
    <button class="add-button"><a href="/new">Add new message</a></button>
    <div class="container">
            <% for (let i = 0; i < messages.length; i++) { %>
                <div class= message-<%= i %>>
                    <h3><%= messages[i].user %></h3>
                    <h5>Created on <%= messages[i].added %></h5>
                    <p><%= messages[i].text %></p>
                    <button>
                        <a href="/message/<%= i %>">Details</a>
                    </button>
                </div>
            <% } %>
    </div>
</body>
</html>